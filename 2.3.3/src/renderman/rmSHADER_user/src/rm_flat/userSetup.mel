print "This is $RM_SHADER_USER/src/rm_flat/userSetup.mel\n";

string $erSHADER_user_DIR = getenv("RM_SHADER_USER");
if( $erSHADER_user_DIR == "" )
{
    error("environment variable <RM_SHADER_USER> is not set in Maya.env");
} else {
    eval( "source \"" + $erSHADER_user_DIR + "/src/rm_flat/AErm_flatTemplate.mel\"" );
}
